#if($generateTime)
/*
 * DON'T EDIT! THIS CODE GENERATED BY tezrok at $generateTime
 */
#end
#if($package)
package $package;
#end

#foreach( $import in $imports )
#if ($import) 
import $import;
#end
#end

#if($comment)
/**
 * $comment
 */
#end
#foreach($annotation in $annotations)
$annotation
#end
$modificator$kind $name $inheritance{
#foreach($field in $fields)
#foreach($annotation in $field.Annotations)
    $annotation
#end
    $field
#end
#foreach($constructor in $constructors)

$constructor
#end
#foreach($method in $methods)

$method
#end
#if($equalsField)

    @Override
    public boolean equals(Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }

        $name $name.toLowerCase() = ($name) o;

        if ($equalsField == null || $name.toLowerCase().$equalsField == null) return false;

        return ${equalsField}.equals($name.toLowerCase().$equalsField);
    }

    @Override
    public int hashCode() {
        return (int) ($equalsField ^ ($equalsField >>> 32));
    }
#end
#if($hasToString)

    @Override
    public String toString() {
        return "$name{" +
#foreach($field in $fields)
                "$field.Name=" + $field.Name + "; " +
#end
            "}";
    }
#end
}