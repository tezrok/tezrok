package ${package}.service;

import ${package}.dto.${name}Dto;
import ${package}.repository.${name}Repository;
import org.jetbrains.annotations.NotNull;
import org.springframework.stereotype.Service;
import org.springframework.data.domain.Page;
import org.springframework.data.domain.Pageable;
import org.springframework.transaction.annotation.Transactional;
import java.util.List;

/**
 * Service for {@link ${name}Dto}.
 */
@Transactional
@Service
public class ${name}Service {
    @NotNull
    protected final ${name}Repository ${lname}Repository;

    public ${name}Service(@NotNull ${name}Repository ${lname}Repository) {
        this.${lname}Repository = ${lname}Repository;
    }

    @Transactional(readOnly = true)
    public List<${name}Dto> findAll() {
        return ${lname}Repository.findAll();
    }

    @NotNull
    public ${name}Dto save(@NotNull ${name}Dto dto) {
        return ${lname}Repository.save(dto);
    }

    public List<${name}Dto> saveAll(@NotNull Iterable<${name}Dto> dtos) {
        return ${lname}Repository.saveAll(dtos);
    }

    @NotNull
    public ${name}Dto update(@NotNull ${name}Dto dto) {
        return ${lname}Repository.update(dto);
    }

    public int deleteById(@NotNull Long id) {
        return ${lname}Repository.deleteById(id);
    }

    @Transactional(readOnly = true)
    public Page<${name}Dto> searchByTerm(String term, Pageable pageable) {
        return ${lname}Repository.searchByTerm(term, pageable);
    }
}
